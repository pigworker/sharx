# Testing Sharx

    does pandoc treat this as code?

\`\`\`
how about this?
\`\`\`

$x^2$

I hope *something* works.

{pipe('send(`hello`); 'send(`world`),`{'recv()} {'recv()}`)}
~~~
pipe(<s>, x) = x
pipe(<'send(x)->s>, <'recv()->r>) = pipe(s([]), r(x))
pipe(v, <r>) = 'abort()